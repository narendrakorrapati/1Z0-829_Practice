package org.enumexample;

public enum Season1 {
    WINTER,
    SPRING,
    SUMMER,
    FALL; //Semicolon optional for simple enums

    //We refer to an enum that only contains a list of values as a simple enum
}
